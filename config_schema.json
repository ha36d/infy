{
    "type": "object",
    "properties": {
        "metadata": {
            "type": "object",
            "properties": {
            "name": { "type": "string" }
            },
            "required": ["name", "org"]
        },
        "cloud": {
            "type": "string",
            "enum": ["aws", "azure", "gcp", "oci"]
        },
        "account": { "type": "string" },
        "region": { "type": "string" }
    },
    "required": ["metadata", "cloud", "account", "region"]
}
